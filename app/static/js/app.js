const nav=document.querySelector('nav'),toggle=document.querySelector('.nav-toggle');if(toggle)toggle.onclick=()=>nav.classList.toggle('open');
function wa(e){e.preventDefault();const b=e.currentTarget;const msg=`Hello Crisviola Resources Ltd,\nI am interested in the following product:\nProduct: ${b.dataset.name}\nListed Price: ${b.dataset.price}\nQuantity: ${b.dataset.unit}\nPlease let me know if this product is available and provide further information about delivery and final pricing.\nThank you.`;window.open(`https://wa.me/${window.WA}?text=${encodeURIComponent(msg)}`,'_blank')}
document.querySelectorAll('.js-wa').forEach(b=>b.addEventListener('click',wa));
const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');obs.unobserve(e.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(e=>obs.observe(e));
const s=document.querySelector('#search');if(s)s.addEventListener('input',()=>{clearTimeout(window.st);window.st=setTimeout(async()=>{const r=await fetch('/api/products?q='+encodeURIComponent(s.value));if(!r.ok)return;const d=await r.json();document.querySelector('#grid').innerHTML=d.map(p=>`<article class="card reveal show"><a class="pic" href="${p.detail_url}">${p.image?`<img src="${p.image}" alt="${p.name}">`:`<div class="placeholder">${p.name[0]}</div>`}<span>${p.availability}</span></a><div class="body"><small>${p.category} · ${p.unit}</small><h3>${p.name}</h3><strong>${p.price}</strong><em>listed price</em><div><a class="btn dark" href="${p.detail_url}">View details</a><a class="btn green js-wa" data-name="${p.name}" data-price="${p.price}" data-unit="${p.unit}" href="#">Order ↗</a></div></div></article>`).join('');document.querySelectorAll('.js-wa').forEach(b=>b.addEventListener('click',wa))},180)});
